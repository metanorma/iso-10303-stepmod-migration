# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: import

on:
  push:
    branches: [ main ]
    # tags:
    #   - '*'
  schedule:
    - cron: '0 1 * * *'  # every day at 1:00

jobs:
  import:
    name: import
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.BOOST_CVS_SSH_PRIVATE_KEY }}
          name: id_rsa # optional
          known_hosts: ${{ secrets.BOOST_CONSEIL_KNOWN_HOSTS }}
          config: |
            Host cvs.boost-lab.net
              User ronald
              IdentityFile ~/.ssh/id_rsa

      - name: Test SSH
        run: |
          ssh ronald@cvs.boost-lab.net 'ls ../'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get -y install cvs-fast-export

      - name: Setup Git
        run: |
          git config --global init.defaultBranch main
          git config --global user.name "metanorma-ci"
          git config --global user.email "metanorma-ci@users.noreply.github.com"

      - name: reposurgeon - Cache reposurgeon
        id: cache-reposurgeon
        uses: actions/cache@v2
        with:
          path: reposurgeon
          key: ${{ runner.os }}-${{ hashFiles('reposurgeon-cache-key') }}

      - name: reposurgeon - Clone and build reposurgeon
        if: steps.cache-reposurgeon.outputs.cache-hit != 'true'
        run: |
          git clone https://gitlab.com/esr/reposurgeon
          cd reposurgeon
          git checkout 2f5f83ec63da34beaf8293434b5e1627366dfa7c
          sed -i 's/apt-get install /apt-get install -y /' Makefile
          make fullinstall
          make all

      - name: reposurgeon - Install reposurgeon
        working-directory: reposurgeon
        run: |
          sudo make install

      - name: rsync to stepmod-rsync/
        run: |
          rsync -avrPz -e ssh ronald@cvs.boost-lab.net:/stepmod/ stepmod-rsync
      #     rsync -avrPz -e ssh ronald@cvs.boost-lab.net:/stepmod/stepmod/data stepmod-rsync
      # - name: Run cvssync to sync to 'stepmod/data'
      #   run: |
      #     cvssync -v ronald@cvs.boost-lab.net:/stepmod stepmod/data

      - name: Convert CVS to Git via reposurgeon
        working-directory: stepmod
        env:
          TERM: xterm-256color
        run: |
          ln -s ../stepmod-rsync/stepmod stepmod-mirror
          make
          mv stepmod-git ../iso-10303-stepmod-cvs

      - name: Add Git remote - GitHub
        working-directory: iso-10303-stepmod-cvs
        run: |
          git remote add origin \
            https://${{ secrets.METANORMA_CI_USERNAME }}:${{ secrets.METANORMA_CI_PAT_TOKEN }}@github.com/metanorma/iso-10303-stepmod-cvs

      - name: Add Git remote - ISO BitBucket
        working-directory: iso-10303-stepmod-cvs
        run: |
          git remote add iso \
            https://${{ secrets.ISO_BITBUCKET_USERNAME_URLENCODED }}:${{ secrets.ISO_BITBUCKET_PAT_TOKEN_URLENCODED }}@sd.iso.org/bitbucket-pilot/scm/~ronald.tse_eccma.org/smrl-metanorma.git

      - name: Git switch branch to main
        working-directory: iso-10303-stepmod-cvs
        run: |
          git checkout main

      # - name: Remove large files from history that have been already deleted
      #   run: |
      #     curl -sSL https://repo1.maven.org/maven2/com/madgag/bfg/1.14.0/bfg-1.14.0.jar -o bfg.jar
      #     java -jar bfg.jar --strip-blobs-bigger-than 100M iso-10303-stepmod-cvs
      #     git -C iso-10303-stepmod-cvs reflog expire --expire=now --all && \
      #       git -C iso-10303-stepmod-cvs gc --prune=now --aggressive

      - name: List git branches
        working-directory: iso-10303-stepmod-cvs
        run: |
          git branch

      - name: List git tags
        working-directory: iso-10303-stepmod-cvs
        run: |
          git tag

      - name: Git push to GitHub iso-10303-stepmod-cvs
        working-directory: iso-10303-stepmod-cvs
        run: |
          max=$(git log --oneline|wc -l); \
          for i in $(seq $max -500 1); do \
            echo $i; g=$(git log --reverse --oneline --skip $i -n1|perl -alne'print $F[0]'); \
            git push origin $g:refs/heads/gitconvert -f; \
          done

          for x in $(git branch | tr -ds '*' '' | paste -d " " - - - - - - - - - - | tr -s ' ' '#'); do \
            y="${x//#/ }"; \
            echo $y; \
            git push origin -f $y; \
          done

          for x in $(git tag | tr -ds '*' '' | paste -d " " - - - - - - - - - - | tr -s ' ' '#'); do \
            y="${x//#/ }"; \
            echo $y; \
            git push origin -f $y; \
          done

          git push origin :refs/heads/gitconvert -f

      - name: Git push to ISO BitBucket smrl-metanorma
        working-directory: iso-10303-stepmod-cvs
        run: |
          max=$(git log --oneline|wc -l); \
          for i in $(seq $max -500 1); do \
            echo $i; g=$(git log --reverse --oneline --skip $i -n1|perl -alne'print $F[0]'); \
            git push iso $g:refs/heads/gitconvert -f; \
          done

          for x in $(git branch | tr -ds '*' '' | paste -d " " - - - - - - - - - - | tr -s ' ' '#'); do \
            y="${x//#/ }"; \
            echo $y; \
            git push iso -f $y; \
          done

          for x in $(git tag | tr -ds '*' '' | paste -d " " - - - - - - - - - - | tr -s ' ' '#'); do \
            y="${x//#/ }"; \
            echo $y; \
            git push iso -f $y; \
          done

          git push iso :refs/heads/gitconvert -f
